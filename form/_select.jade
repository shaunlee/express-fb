- var id = id || name, value = form && form[name] || value || '', options = options || [], classes = classes || ''
select(id=id, name=name, class=classes)
  - if (options instanceof Array)
    - each option in options
      option(value=option, selected=value === option ? 'selected' : '')= option
  - else
    - for (var option in options)
      option(value=option, selected=value === option ? 'selected' : '')= options[option]
!= partial('form/errors', {name: name})
