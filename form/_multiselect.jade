- var id = id || name, value = form && form[name] || value || [], options = options || [], classes = classes || null
select(id=id, name=name, multiple='multiple', class=classes)
  - if (options instanceof Array)
    - each option in options
      option(value=option, selected=value.indexOf(option) > -1 ? 'selected' : null)= option
  - else
    - for (var option in options)
      option(value=option, selected=value.indexOf(option) > -1 ? 'selected' : null)= options[option]
!= partial('form/errors', {name: name})
