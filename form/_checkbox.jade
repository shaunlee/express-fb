- var id = id || name, value = form && form[name] || value || '', checked = checked ? 'checked' : null, classes = classes || null
- var hideErrors = false || hideErrors
input(type='checkbox', id=id, name=name, value=value, checked=checked, class=classes)
- if (!hideErrors)
  != partial('form/errors', {name: name})
