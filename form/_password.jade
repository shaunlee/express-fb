- var id = id || name, placeholder = placeholder || null, classes = classes || null
input(type='password', id=id, name=name, placeholder=placeholder, class=classes)
!= partial('form/errors', {name: name})
